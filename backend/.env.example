DATABASE_URL=postgresql+asyncpg://postgres:password@localhost/jamidusu

# 커넥션 풀 (PostgreSQL 전용). 키우기 전에 app/database.py 의 계산식을 읽을 것:
#   (DB_POOL_SIZE + DB_MAX_OVERFLOW) × 워커 수 × 인스턴스 수 + 5 ≤ DB 커넥션 상한
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=5
# 풀에서 커넥션을 기다리는 최대 시간(초). 초과하면 503 + Retry-After 로 응답한다.
DB_POOL_TIMEOUT_SECONDS=10
# 풀러 뒤의 죽은 커넥션을 재사용하지 않도록 주기적으로 버린다(초).
DB_POOL_RECYCLE_SECONDS=300
# 서버측 statement_timeout(ms). 폭주 쿼리가 커넥션을 붙잡지 못하게 한다. 0 이면 미설정.
DB_STATEMENT_TIMEOUT_MS=15000

KAKAO_CLIENT_ID=your_kakao_rest_api_key
KAKAO_CLIENT_SECRET=your_kakao_client_secret
KAKAO_REDIRECT_URI=http://localhost:8000/auth/kakao/callback

SECRET_KEY=change-this-to-a-random-secret
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Comma-separated. First entry = OAuth redirect target. All entries are CORS-allowed.
# Production example: FRONTEND_URLS=https://zami.vercel.app,http://localhost:3000
FRONTEND_URLS=http://localhost:3000

DEBUG=true

# Required for embedding (ingestion + retrieval) and translation scripts.
OPENAI_API_KEY=sk-your-openai-api-key

# Optional. LLM 응답(사주 풀이 등) 서버 캐시. 비워두면 캐시 없이 동작.
# 예: redis://localhost:6379/0 또는 rediss://default:password@host:6379
REDIS_URL=

# Optional. 비워두면 Sentry 초기화를 건너뛴다.
SENTRY_DSN=

# Optional. 토스페이먼츠 시크릿 키. 비워두면 결제 승인이 동작하지 않고
# /payments/test-topup(테스트용 무료 충전)이 열린다.
TOSS_SECRET_KEY=

# Optional. FCM 푸시 발송용 서비스 계정 JSON "전체 내용". 비워두면 푸시 비활성화.
FIREBASE_SERVICE_ACCOUNT_JSON=

# Optional. 카카오 unlink(탈퇴) 용 어드민 키.
KAKAO_ADMIN_KEY=

# Optional. 온보딩 완료(가입) 알림 메일. 셋 다 비우면 알림만 꺼지고 온보딩은 그대로 동작한다.
# ALERT_EMAIL_API_KEY 는 Resend(https://resend.com) API 키.
# ALERT_EMAIL_FROM 은 Resend 에서 도메인을 인증하기 전이라면 onboarding@resend.dev 를 쓴다
# (이 발신 주소는 Resend 계정 소유자 본인 메일로만 배달된다).
ALERT_EMAIL_API_KEY=
ALERT_EMAIL_TO=
ALERT_EMAIL_FROM=MeloBe 알림 <onboarding@resend.dev>

# 테스트용. true 면 매칭 후보에서 이성 필터를 제거해 동성도 매칭된다.
# 운영에서는 반드시 false(또는 미설정)로 둘 것.
ALLOW_SAME_GENDER_MATCH=false